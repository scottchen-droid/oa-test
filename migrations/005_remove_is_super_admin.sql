-- ================================================================
-- Migration 005: 移除 is_super_admin 欄位
-- 說明: 系統改以純 RBAC（角色與權限）控管存取，移除繞過所有權限的超管旗標。
--       管理員帳號改以持有 ADMIN 角色（含全部權限）實現相同效果。
-- ================================================================

BEGIN;

ALTER TABLE users DROP COLUMN IF EXISTS is_super_admin;

COMMIT;
